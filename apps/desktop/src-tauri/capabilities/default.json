{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main OrbitLab window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
